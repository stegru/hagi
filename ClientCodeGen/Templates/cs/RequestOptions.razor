@using System.Linq
@using ClientCodeGen.Models
@inherits RequestOptionsTemplate

// <auto-generated/>

namespace GuestClient
{
    using System;
    using HagiShared.Api;

    public partial class RequestOptions {

        private static Type[] GeneratedTypes = {
            @string.Join(",\n", this.AllRequests.Select(r => $"typeof({r.RequestType.Name}Options)"))
        };
    }
@foreach (RequestModel requestModel in this.AllRequests)
{
    this.Include<RequestTemplate>(requestModel);
}

}