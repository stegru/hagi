@using System.Linq
@using ClientCodeGen.Models
@inherits RequestOptionsTemplate

// <auto-generated/>

namespace Hagi.HagiGuest
{
    using System;
    using Hagi.Shared.Api;

    public partial class RequestOptions {

        public static readonly Type[] GeneratedTypes = {
@string.Join(",\n", GuestRequests.Select(r => $"typeof({r.RequestType.Name}Options)"))
};
    }
@foreach (RequestModel requestModel in GuestRequests)
{
    Include<RequestTemplate>(requestModel);
}

}